import { Router } from 'express';

const router = Router();

// Internal authenticated routes will be added here as features are implemented
// Example: CRUD operations, user management, etc.

export default router;
