import { Router } from 'express';

const router = Router();

// Public routes will be added here as features are implemented
// Example: Authentication, public data access, etc.

export default router;
